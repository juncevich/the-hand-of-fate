name: Bot CI

on:
  push:
    branches: [main]
    paths:
      - bot/**
      - proto/**
      - .github/workflows/bot.yml
  pull_request:
    paths:
      - bot/**
      - proto/**
      - .github/workflows/bot.yml

concurrency:
  group: bot-${{ github.ref }}
  cancel-in-progress: true

jobs:

  bot-test:
    name: Bot — Test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history so `buf breaking` can diff against the PR base branch below.
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: bot/go.mod

      - uses: bufbuild/buf-setup-action@v1

      - name: Lint proto
        run: buf lint

      # Catches proto changes that would break the deployed backend or bot,
      # since they deploy independently on separate schedules/pipelines.
      - name: Check for breaking proto changes
        if: github.event_name == 'pull_request'
        run: buf breaking --against ".git#branch=origin/${{ github.base_ref }}"

      - name: Install proto plugins
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest

      - name: Generate proto
        run: buf generate

      - name: Download dependencies
        run: cd bot && go mod download

      - name: Check formatting
        run: test -z "$(gofmt -l bot)"

      - run: cd bot && go vet ./...
      - run: cd bot && go run honnef.co/go/tools/cmd/staticcheck@latest ./...
      - run: cd bot && go test ./...

  build-bot:
    name: Bot — Build binary (linux/amd64)
    runs-on: ubuntu-24.04
    needs: bot-test
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: bot/go.mod

      - uses: bufbuild/buf-setup-action@v1

      - name: Install proto plugins
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest

      - name: Generate proto
        run: buf generate

      - name: Download dependencies
        run: cd bot && go mod download

      - name: Build
        run: |
          cd bot
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
            go build -ldflags="-s -w" -o fate-bot ./cmd/bot

      - uses: actions/upload-artifact@v4
        with:
          name: bot-binary
          path: bot/fate-bot
          if-no-files-found: error
