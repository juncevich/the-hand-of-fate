apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: hand-of-fate
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 9090
      targetPort: 9090
  type: ClusterIP
